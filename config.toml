max_level = 1000

[physics]
target_vx = 30.0
accel_ground = 200.0
accel_air = 80.0
jump_vy = -28.0
gravity = 80.0
coyote_time = 0.1
jump_buffer_time = 0.1
jump_release_gravity_mult = 3.0
friction = 400.0

[keys]
left = ["Left", "a", "A"]
right = ["Right", "d", "D"]
jump = ["Up", "w", "W", "Space"]
quit = ["Esc", "q", "Q"]
restart = ["Enter"]
